<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Item</title>
    <style>
        body {
            background: linear-gradient(to right, #add8e6, #87cefa);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        form {
            background-color: #3b2f36;
            color: white;
            padding: 20px;
            border-radius: 8px;
            width: 300px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.5);
        }

        h1  {
            text-align: center;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            margin-bottom: 15px;
        }

        .form-control {
            margin-top: 5px;
        }

        button {
            background-color: white;
            color: #3b2f36;
            border: none;
            padding: 10px;
            text-align: center;
            text-decoration: none;
            display: block;
            font-size: 16px;
            margin: auto;
            cursor: pointer;
            border-radius: 8px;
        }
    </style>
</head>
<body>
    <form action="/update-item" method="POST">
        <h1>Update Item</h1>
        <div class="form-group">
            <label for="materialName">Material Name:</label>
            <input type="text" class="form-control" id="materialName" name="materialName" required>
        </div>
        <div class="form-group">
            <label for="price">Price:</label>
            <input type="text" class="form-control" id="price" name="price" pattern="^\d*\.?\d+$" required>
        </div>
        <button type="submit">Update</button>
    </form>

    <form action="/delete-item" method="POST" style="margin-top: 20px;">
        <h1>Delete Item</h1>
        <div class="form-group">
            <label for="deleteMaterialName">Material Name:</label>
            <input type="text" class="form-control" id="deleteMaterialName" name="materialName" required>
        </div>
        <button type="submit" style="background-color: #1a73e8; color: white; border: none; padding: 10px 20px; text-align: center; text-decoration: none; display: inline-block; font-size: 16px; margin: 4px 2px; cursor: pointer; border-radius: 8px;">Delete 🗑️</button>
    </form>
</body>
</html>